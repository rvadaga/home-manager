{
  "hooks": {
    "PreToolUse": [
      {
        "matcher": "Bash",
        "hooks": [
          {
            "type": "command",
            "command": "cmd=$(jq -r '.tool_input.command // empty'); if printf '%s' \"$cmd\" | grep -qE '(^|[[:space:]/])brew[[:space:]]+(install|reinstall|tap)([[:space:]]|$)'; then printf '%s' '{\"hookSpecificOutput\":{\"hookEventName\":\"PreToolUse\",\"permissionDecision\":\"ask\",\"permissionDecisionReason\":\"prefer nix: homebrew is a last resort. add the package to the nix config and rebuild (nixpkgs home.packages/systemPackages for cli tools, or homebrew.brews/casks for brew-only). only confirm this brew command if nix genuinely cannot provide it.\"}}'; fi"
          }
        ]
      }
    ]
  },
  "permissions": {
    "allow": [
      "Bash(brew update)"
    ]
  }
}
